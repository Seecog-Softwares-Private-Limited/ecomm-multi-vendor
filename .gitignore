# Dependencies

# Next.js — build output (never commit; run `next build` on the server or in CI)
.next/

out/

# Production
# build/

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Local env files
.env
.env*.local
.env.development.local
.env.test.local
.env.production.local
properties.env

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# IDE / Editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Prisma
prisma/migrations/**/migration_lock.toml

# Testing
coverage/
.nyc_output/

# Local Linux deploy bundle (large — scp to server; do not commit)
dist/ecomm-server-bundle/
dist/ecomm-linux-server-bundle.tar.gz

# Misc
*.pem
node_modules

# Runtime uploads (product images, KYC) — never commit; live on the server under PUBLIC_UPLOAD_ROOT
public/uploads/**
!public/uploads/.gitkeep
